{
  "name": "comunidade",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute escola --local --file=db/schema.sql",
    "db:remote": "wrangler d1 execute escola --remote --file=db/schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "wrangler": "^4.129.0",
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^5.6.3"
  }
}
